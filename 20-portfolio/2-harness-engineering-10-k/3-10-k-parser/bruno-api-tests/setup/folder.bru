meta {
  name: Setup
  type: folder
}

docs {
  Seeds the parser backend for LangGraph retrieval tests.
  Run these requests before langgraph stream assertions so Neo4j contains a mock filing graph.
}
