meta {
  name: LangGraph Runtime
  type: folder
}

docs {
  Verifies the internal LangGraph-style runtime endpoints:
  thread creation, retrieval-backed streaming run, snapshot inspection, and stream resume.
}
