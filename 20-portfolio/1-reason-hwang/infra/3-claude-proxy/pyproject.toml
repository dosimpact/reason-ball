[project]
name = "claude-code-proxy"
version = "0.1.0"
description = "Local OpenAI-compatible proxy backed by Claude Code CLI print mode"
requires-python = ">=3.10"
dependencies = [
    "aiohttp~=3.11.13",
    "openai>=1.0.0",
]

[tool.uv]
package = false
