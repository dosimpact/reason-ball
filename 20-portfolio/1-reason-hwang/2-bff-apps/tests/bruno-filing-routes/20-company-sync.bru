meta {
  name: company-sync
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/company-sync-jobs
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

assert {
  res.status: eq 201
}

tests {
  test("BFF-DIR-001 company-sync", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().syncedCount).to.equal(1);
  });
}
