meta {
  name: removed-7
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/filing-sync-jobs
  body: none
  auth: none
}

assert {
  res.status: eq 404
}

tests {
  test("SEC-API-NAME-001 removed-7", function() {
    expect(res.getStatus()).to.equal(404);

  });
}
