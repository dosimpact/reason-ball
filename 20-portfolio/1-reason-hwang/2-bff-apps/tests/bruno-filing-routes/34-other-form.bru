meta {
  name: other-form
  type: http
  seq: 34
}
get {
  url: {{baseUrl}}/filings?cik=6&formType=10-Q
  body: none
  auth: none
}
assert {
  res.status: eq 200
}
tests {
  test("SEC-AMEND-001 other-form", function() {
    const body = res.getBody(); const item = body.items[0];
    expect(item.amendments).to.have.length(0);
  });
}
