meta {
  name: raw-plain
  type: http
  seq: 42
}
get {
  url: {{baseUrl}}/filings/11/0000000011-26-000003/content
  body: none
  auth: none
}
assert {
  res.status: eq 200
}
tests {
  test("SEC-CONTENT-001 plain", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getHeader("content-type")).to.equal("text/plain; charset=utf-8");
    expect(res.getHeader("content-security-policy")).to.contain("sandbox;");
    expect(res.getHeader("x-content-type-options")).to.equal("nosniff");
    expect(res.getBody()).to.equal("공시 plain text");
  });
}
