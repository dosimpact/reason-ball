meta {
  name: latest
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/all-company-filing-sync-jobs/latest
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("SEC-API-NAME-001 latest", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().runId).to.equal(bru.getVar("runId"));
  });
}
