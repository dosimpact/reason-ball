meta {
  name: all
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/all-company-filing-sync-jobs
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {"years": 20, "refreshArchive": false}
}

assert {
  res.status: eq 202
}

tests {
  test("SEC-API-NAME-001 all", function() {
    expect(res.getStatus()).to.equal(202);
    expect(res.getBody().runId).to.be.a("string"); bru.setVar("runId", res.getBody().runId);
  });
}
