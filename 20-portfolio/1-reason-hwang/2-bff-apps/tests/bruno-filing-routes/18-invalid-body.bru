meta {
  name: invalid-body
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/company-filing-sync-jobs
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  []
}

assert {
  res.status: eq 400
}

tests {
  test("BFF-DIR-001 invalid-body", function() {
    expect(res.getStatus()).to.equal(400);

  });
}
