meta {
  name: invalid-17
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/companies?limit=bad
  body: none
  auth: none
}

assert {
  res.status: eq 400
}

tests {
  test("SEC-COMPANIES-PAGE-001 invalid-17", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
