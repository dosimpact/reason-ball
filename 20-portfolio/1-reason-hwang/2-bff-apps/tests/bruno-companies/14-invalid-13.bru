meta {
  name: invalid-13
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/companies?page=-1
  body: none
  auth: none
}

assert {
  res.status: eq 400
}

tests {
  test("SEC-COMPANIES-PAGE-001 invalid-13", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
