meta {
  name: invalid-15
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/companies?page=2abc
  body: none
  auth: none
}

assert {
  res.status: eq 400
}

tests {
  test("SEC-COMPANIES-PAGE-001 invalid-15", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
