meta {
  name: Delete Thread
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/threads/{{threadId}}
  body: none
}

assert {
  res.status: eq 200
}

tests {
  test("BRUNO-THREAD-01: deletes the lifecycle thread", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
