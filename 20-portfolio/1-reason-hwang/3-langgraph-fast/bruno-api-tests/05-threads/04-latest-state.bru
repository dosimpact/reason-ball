meta {
  name: Get Latest Thread State
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/threads/{{threadId}}/state
  body: none
}

assert {
  res.status: eq 200
}

tests {
  test("BRUNO-THREAD-02: returns latest state", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
