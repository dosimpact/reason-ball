meta {
  name: Join Existing Run
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/threads/{{threadId}}/runs/{{runId}}/join
  body: none
}

assert {
  res.status: eq 200
}

tests {
  test("BRUNO-RUN-01: joins an existing run", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
