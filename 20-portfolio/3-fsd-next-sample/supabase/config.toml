# CLI metadata for remote migrations and explicit seed application.
# Runtime Auth, Storage and API settings are managed in the Supabase dashboard.
project_id = "character-english-ai"

[db.migrations]
enabled = true
schema_paths = []

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]
