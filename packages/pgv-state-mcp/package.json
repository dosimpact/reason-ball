{
  "name": "@reason-ball/pgv-state-mcp",
  "version": "0.1.0",
  "description": "PGV state MCP server for apb-pgv Plan -> Gradate -> Validate workflows",
  "private": true,
  "main": "release/latest.js",
  "scripts": {
    "build": "pnpm run release",
    "release": "node scripts/build.mjs",
    "start": "node release/latest.js",
    "test": "pnpm run release && node scripts/test.mjs"
  },
  "keywords": [
    "pgv",
    "mcp",
    "codex",
    "agentic-playbook"
  ],
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
