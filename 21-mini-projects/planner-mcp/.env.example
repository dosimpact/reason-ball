# Optional Figma personal access token with file_content:read scope.
# Copy to .env.local; never commit a real token.
FIGMA_ACCESS_TOKEN=

# Comma-separated LAN server hostnames/IPs, without protocol or port.
PLANNER_ALLOWED_HOSTS=192.168.0.45
