# 주요 화면 URL 연결

- 요청: 작업 공간·템플릿 관리·AI 작업 안내의 독립 URL.
- 결정: `/`, `/templates`, `/ai-workflow`; 기존 `/mcp-guide` 유지. 공통 layout이 프로젝트 선택을 유지하고 경로로 화면을 결정한다.
- 메뉴를 Link로 전환, 활성 메뉴 aria-current 표시, 미저장 메뉴 이동 확인 유지.
- stock: 비즈니스 설계 및 시스템 설계의 화면 URL.
- 검증: 진행 중.

## 완료 검증

- `pnpm test:e2e`: Next/Storybook 빌드 성공, Bruno 25/25, Playwright 27/27 통과. 새 URL 회귀와 기존 미저장 보호·MCP·문서·템플릿 흐름 포함.
- `pnpm lint` 통과. 타입 검사는 production build에서 통과.
- 외부 `http://dodonet.iptime.org:14000`에서 네 경로 직접 접속 200 및 각 메뉴 활성 상태, 로고 홈 이동 확인.
- 4000번 Planner 프로세스의 작업 경로 확인 후 새 빌드로 교체. 기존 사용자 데이터 유지.
- 실행 증거: `/tmp/planner-page-urls-e2e.log`(로컬 비추적).
- 범위: 화면 URL 연결. 프로젝트/문서 선택의 새로고침 복원이나 개별 문서 permalink는 포함하지 않음.
