meta {
  name: 04-verification-ai-second
  type: http
  seq: 10
}
patch {
  url: {{baseUrl}}/api/documents/{{documentId}}/checklist/{{secondId}}
  body: json
  auth: none
}

body:json {
  {
    "aiResult": "passed",
    "expectedRevision": 3
  }
}

assert {
  res.status: eq 200
}
tests {
  test("ai-second", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().revision).to.equal(4);
  });
}
