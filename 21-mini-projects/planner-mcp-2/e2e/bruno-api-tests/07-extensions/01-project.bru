meta {
  name: extension-project
  type: http
  seq: 1
}
post {
  url: {{baseUrl}}/api/projects
  body: json
  auth: none
}
body:json {
  {
    "title": "Extensions HTTP"
  }
}
assert {
  res.status: eq 200
}
tests {
  test("project", function() { bru.setVar('extensionProjectId',res.getBody().id); });
}
