meta {
  name: project
  type: http
  seq: 1
}
post {
  url: {{baseUrl}}/api/projects
  body: json
  auth: none
}
body:json {
  {
    "title": "CodeWeave API"
  }
}
assert {
  res.status: eq 200
}
tests {
  test("project", function() { bru.setVar('cwProject', res.getBody().id); });
}
